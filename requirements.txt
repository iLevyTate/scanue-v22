# Runtime dependencies, kept in sync with [project.dependencies] in
# pyproject.toml -- which is the source of truth. This file remains so that
# `pip install -r requirements.txt` keeps working for existing setups.
#
# Prefer:  pip install -e .          (runtime)
#          pip install -e ".[dev]"   (adds pytest, ruff, mypy)
langchain-core>=1.0,<2.0
langgraph>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-ollama>=1.0,<2.0
langchain-huggingface>=1.0,<2.0
openai>=2.0,<3.0
pydantic>=2.0,<3.0
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0
pytest>=8.0,<10.0
pytest-asyncio>=0.23,<2.0
